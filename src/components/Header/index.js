import React from 'react';

import './style.scss';

const Header = (() => (
  <div className="navList--box">
    <div className="navList--image" />
  </div>
));

Header.propTypes = {

};

export default Header;
